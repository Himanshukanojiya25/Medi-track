export enum PrescriptionPermissions {
  VIEW_PRESCRIPTION = 'prescription:view',
  CREATE_PRESCRIPTION = 'prescription:create',
  UPDATE_PRESCRIPTION = 'prescription:update',
  CANCEL_PRESCRIPTION = 'prescription:cancel',
}
