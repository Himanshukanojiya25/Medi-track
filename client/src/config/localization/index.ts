export {
  SUPPORTED_LOCALES,
  DEFAULT_LOCALE,
  FALLBACK_LOCALE,
} from "./locales.config";

export {
  SUPPORTED_CURRENCIES,
  DEFAULT_CURRENCY_BY_LOCALE,
  CURRENCY_FORMAT_CONFIG,
} from "./currency.config";
